# Author: petter.strandmark@gmail.com (Petter Strandmark)

FILE(GLOB SUBMODULAR_INTERNAL_SRC *.cpp)

SET(SUBMODULAR_LIBRARY_SOURCE
	${SUBMODULAR_INTERNAL_SRC}
	${SUBMODULAR_HEADERS})

ADD_LIBRARY(pseudoboolean ${SUBMODULAR_LIBRARY_SOURCE})

INSTALL(TARGETS pseudoboolean
        RUNTIME DESTINATION bin
        LIBRARY DESTINATION lib
        ARCHIVE DESTINATION lib)
